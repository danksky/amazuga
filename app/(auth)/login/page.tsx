import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function LoginPage() {
  return (
    <PlaceholderPage
      eyebrow="Auth"
      title="Sign in"
      description="Authentication will support saved properties, UPI search behavior, onboarding status, and portal access."
    />
  );
}
